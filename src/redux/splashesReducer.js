import * as ActionTypes from './ActionTypes';
import oSplashesData from '../shared/data/splashes';

export const SplashesReducer = (oState = oSplashesData, oAction) => {
   switch(oAction.type) {  
      default:
         return oState;
   }
}